{
  "name": "cursor-link-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local helper that lets the Cursor Link Minecraft mod talk to a Cursor agent on this computer.",
  "scripts": {
    "start": "tsx src/server.ts",
    "smoke": "tsx scripts/smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.6"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
